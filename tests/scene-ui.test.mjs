import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {createHash} from 'node:crypto';
const read=name=>JSON.parse(readFileSync(new URL('../lib/'+name,import.meta.url)));
test('all opening chapter scenes and supported inventory pixels keep exact admitted hashes',()=>{const scenes=read('stage-scene-data.json');assert.deepEqual(Object.values(scenes.chapters).map(x=>x.background),['Bg_Village_01','Bg_Field_01','Bg_Village_01','Bg_Village_01','Bg_City_01','Bg_Level_03']);for(const row of [...scenes.files,...Object.values(read('inventory-icon-data.json')),...Object.values(read('achievement-ui-data.json'))])assert.equal(createHash('sha256').update(readFileSync(new URL('../public/assets/'+row.src,import.meta.url))).digest('hex'),row.sha256);assert.equal(Object.keys(read('inventory-icon-data.json')).length,101);assert.equal(read('inventory-icon-exceptions.json').length,7)});
