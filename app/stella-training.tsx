import {Button} from '@/components/ui/button';
import {stellaRule,stellaState,stellaEntry,stellaPlan,stellaAction,stellaActivation,STELLA_IDLE_PER_DAY,SPIRIT_SHARD_ITEM} from '@/lib/stella.mjs';
import {bondedPower} from '@/lib/adventure.mjs';
export default function StellaTraining({id,game,action,locked}:any){const p=stellaRule(id);if(!p)return <p className="small-note item-status">Stella is not available for this Fellow yet.</p>;const t=stellaState(game),e=stellaEntry(game,id),current=bondedPower(game,id),crossover=p.type===null,pooled=p.itemId===SPIRIT_SHARD_ITEM,typed=!crossover&&p.levels.at(-1).percent>0,currency=crossover?'crossover shards':pooled?'village shards':'owner fragments',sink=p.levels.reduce((n:any,r:any)=>n+r.cost,0),run=(a:string,count:any=1)=>action(a,id,{seq:t.seq,count});return <section className="school-card"><h3>{p.name} · Stella {e?e.level:'inactive'}</h3><p>{(t.stock[p.itemId]||0).toLocaleString()} {currency} · {typed?`${p.type} Power +${e?.percent||0}%`:'own Power only, no typed bonus'}</p><p>Own flat Power +{(e?.flat||0).toLocaleString()} · {p.levels.length} upgrade levels · {sink.toLocaleString()} {currency} for the whole ladder</p>{!e?<><Button disabled={locked} onClick={()=>run('stellaActivate')}>Activate private Stella · Free</Button><details className="rules-note"><summary>About these rules</summary><p>{typed?`Activation grants +${stellaActivation(id)?.percent||0}% ${p.type} Power — the original's own rank-0 value.`:'Activation is free and grants nothing; it opens the paid ladder.'}{pooled||crossover?` One shared ${crossover?'crossover':'village'} shard pool serves every ${crossover?'crossover Fellow':'Fellow without a private fragment'}, so shards spent here are shards another Fellow cannot spend.`:''}</p></details></>:<div className="business-actions">{[1,5,'max'].map(count=>{const plan=stellaPlan(game,id,count),next=plan.rows.at(-1),preview=plan.rows.length?stellaAction(game,'stellaUpgrade',id,{seq:t.seq,count})?.state:null;return <Button key={count} disabled={locked||!next} onClick={()=>run('stellaUpgrade',count)}>{count==='max'?'Upgrade max':'Upgrade up to '+count} · {plan.cost} {pooled||crossover?'shards':'fragments'}{next?` → S${next.level}`:''}{preview?` · +${(bondedPower(preview,id)-current).toLocaleString()} Power`:''}</Button>})}</div>}<p className="small-note item-status">{(t.stock[p.itemId]||0).toLocaleString()} {currency} held · {STELLA_IDLE_PER_DAY}/day from idle play, doubled by your habit multiplier</p><details className="rules-note"><summary>Stella effects and source boundaries</summary>{typed?null:<p>This Fellow’s Stella is a flat-Power track: it raises her own Power and nothing else. That is the original’s own answer — of the 126 characters with a Stella table, only four grant “All &lt;Type&gt; Fellow Power +%”, and the rest grant their owner’s own Power. Giving everybody a typed bonus would be this project’s invention, and because typed bonuses are summed across a type it would multiply every Fellow of that type many times over.</p>}{pooled?<p>Every Fellow without a private fragment item draws on ONE shared village shard pool at the same {STELLA_IDLE_PER_DAY}/day, so the supply does not grow with the roster and a shard spent here is a shard another Fellow cannot spend. The ladder’s own cost, rank count and Power values are this character’s own, imported from the original.</p>:null}{crossover?<p>A crossover Fellow’s Stella is a flat-Power track only: it carries Angie’s own recorded fragment costs and flat values with no typed bonus at all, because a crossover character has no Stella table of its own and a typed bonus would be this project’s invention. Every crossover Fellow draws on ONE shared shard pool at the same {STELLA_IDLE_PER_DAY}/day, so the supply does not grow with the roster.</p>:null}<p>Level values replace the previous totals; they do not add every row together. Ranks, costs, flat Power and typed percents are the original’s own, recovered from its HeroSpirit table rather than from community pages. Rani, Liz, Lucoa and Elise all receive +2% at activation, which is the original’s own rank-0 value; Lucoa carries Angie’s recorded Informed ladder after Angie left the roster. Saved activations retain their recorded values. Our local Power order follows the original: the typed percent multiplies first, then the owner’s own flat Power is added. The original grants some characters bonuses Everkai does not yet model — an own-Power percent, an appointment-yield bonus, a talent-cap raise and hero-group bonuses — and those remain pending.</p></details></section>}
