import {useState} from 'react';
import {familyScene} from '@/lib/family-scenes.mjs';
export default function FamilyArtStage({person,context='profile',large=false}:any){const scene=familyScene(person,context),[failed,setFailed]=useState(false),bounds=scene?.mode==='embedded'?scene.alphaBounds:null;return <div className={'family-art-stage'+(large?' family-art-large':'')+(scene?' scene-'+scene.mode:' scene-fallback')} data-character={person.id} data-model={scene?.model||''}>{scene?.background&&!failed&&<img className="family-scene-background" src={'./assets/'+scene.background} alt="" onError={()=>setFailed(true)}/>}{bounds&&scene?.dimensions?<svg className="family-scene-portrait" role="img" aria-label={person.name+' full character art'} viewBox={[bounds[0],bounds[1],bounds[2]-bounds[0],bounds[3]-bounds[1]].join(' ')} preserveAspectRatio="xMidYMid meet"><image href={'./assets/'+person.art} width={scene.dimensions[0]} height={scene.dimensions[1]}/></svg>:<img className="family-scene-portrait" src={'./assets/'+person.art} alt={person.name+' full character art'}/>}</div>}
