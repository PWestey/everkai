import {Button} from '@/components/ui/button';
import {originalProgression} from '@/lib/original-progression.mjs';
import {staffingStatus,staffingPlan,staffingRule} from '@/lib/staffing.mjs';
const num=(n:number)=>Number.isFinite(n)?n.toLocaleString(undefined,{maximumFractionDigits:0}):'Beyond supported gold';
export default function PaidStaffing({game,id,action,locked}:any){
 const b=game.enterprises[id],p=staffingStatus(id,b),active=originalProgression(game),stock=game.staffingMaterials?.stock||0;
 return <section aria-label="Paid staffing"><h3>Paid hiring & quality</h3>{!active?<p>Choose APK growth in Fellows → Training Rules to use recovered hiring costs and quality progression.</p>:!p?<><p>Hire with gold and improve quality with building materials. Your {num(b.employees)} employees stay, with no charge for past hires and no change to current income.</p><Button disabled={locked} onClick={()=>action('startPaidStaffing',id)}>Start paid progression</Button></>:<><p>Quality {p.quality} · capacity {num(p.cap)} · earned quality bonus +{num(p.bonus*100)}%</p><p>Gold available: {num(game.gold)}. Paid hires: {num(p.paid)} · spent {num(p.gold)} gold.</p><div className="business-actions">{[1,10,'max'].map(n=>{const plan=staffingPlan(game,id,n);return <Button key={n} variant="outline" disabled={locked||!plan.count||plan.cost>game.gold} onClick={()=>action('paidStaffHire',id,n)}>Hire {n==='max'?'budget max':`+${plan.count}`} · {num(plan.cost)} gold{n==='max'?` · ${num(plan.count)} employees`:''}</Button>})}</div><p>Batch prices use the whole batch. Ten singles may cost a different amount.</p><p>Building upgrade materials: {num(stock)}</p>{p.quality<26?<><p>Next quality: capacity {num(staffingRule(id,p.quality+1).cap)} · another +{num((staffingRule(id,p.quality+1).yieldRise-staffingRule(id,p.quality).yieldRise)/100)}% earnings.</p><Button disabled={locked||stock<staffingRule(id,p.quality).cost} onClick={()=>action('upgradeStaffQuality',id)}>Upgrade quality · {num(staffingRule(id,p.quality).cost)} materials</Button></>:<p>Final quality reached.</p>}<Button variant="outline" disabled={locked||stock>999900} onClick={()=>action('claimStaffingMaterials')}>Take 100 building materials · Sandbox</Button><p className="small-note">Free hiring and Hire Cards still stop at 5,000. They grant any capacity needed to keep those employees, with no extra quality income. Only material-paid quality steps add an earnings bonus. Existing employees and entry quality are never billed.</p><details><summary>Progression record</summary><p>Started with {num(b.apkStaffing.baselineEmployees)} employees at compatibility quality {b.apkStaffing.baselineQuality}. Since then: {num(p.free)} free employees, {num(p.paid)} paid employees, {num(p.spent)} building materials spent.</p><p>Original quality costs and yield steps are recovered client data. Free supplies and upgrades before filling capacity are sandbox choices. Hiring discounts and remaining original earnings modifiers are not yet modeled.</p></details></>}</section>;
}
